{
  "name": "trial-expiry-reminder",
  "version": "1.0.0",
  "description": "Zero-cost serverless trial expiry reminder app (AWS SAM + Lambda + DynamoDB + Gmail SMTP)",
  "type": "module",
  "license": "MIT",
  "scripts": {
    "test": "node --test tests/*.test.mjs"
  },
  "dependencies": {
    "@aws-sdk/client-dynamodb": "^3.1121.0",
    "@aws-sdk/lib-dynamodb": "^3.1121.0",
    "nodemailer": "^9.0.6",
    "uuid": "^14.0.2"
  }
}
